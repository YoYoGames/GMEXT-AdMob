/**
 * @function admob_settings_set_volume
 * @desc Sets the app-level volume for video ads (rewarded video, and any video content in
 * interstitial/rewarded interstitial/app open/banner ads). Applies to ads loaded after this call.
 * @param {Real} value The volume, from `0` (silent) to `1` (full volume).
 * @function_end
 */

/**
 * @function admob_settings_set_muted
 * @desc Sets the app-level mute state for video ads. Applies to ads loaded after this call.
 * @param {Bool} muted `true` to mute video ads.
 * @function_end
 */

/**
 * @module settings
 * @title Settings
 * @desc App-wide audio settings for video ads.
 *
 * @section_func
 * @ref admob_settings_set_volume
 * @ref admob_settings_set_muted
 * @section_end
 *
 * @module_end
 */
